git bash installation
cd c:
mkdir
cd <dir>
git init
git config --global user.name "-----"
git config --global user.email "-----"
touch <filename>
git add .
git status
git commit -m "-----"
git status
git log
git cloud account creation
git remote add origin <url>
git clone <url>
git push -u <url>
git pull origin master

git branch
git branch <branchname>
git checkout <branchname>
git branch
git merge <branchname>
git branch -d <branchname>

.gitignore
vi---esc:wq
git mergetool
git reset
git reset --soft HEAD~1
git reset --hard HEAD~1
reset
1. normal reset
file-->changes-->add-->reset-->changes

2. reset
file-->achanges-->dd-->commit-->reset(soft)-->add
file-->changes-->add-->commit-->reset(hard-->changes
